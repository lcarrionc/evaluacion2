<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Listado de Marcas</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

  <div class="max-w-5xl mx-auto py-10">
    <h1 class="text-3xl font-bold mb-6 text-center">Marcas Registradas</h1>

    <div class="mb-4 flex justify-end">
      <a href="/marcas/new" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">
        + Nueva Marca
      </a>
    </div>

    <table class="min-w-full bg-white rounded shadow">
      <thead>
        <tr>
          <th class="py-2 px-4 border-b">#</th>
          <th class="py-2 px-4 border-b">Nombre</th>
          <th class="py-2 px-4 border-b">País de Origen</th>
          <th class="py-2 px-4 border-b">Imagen</th>
          <th class="py-2 px-4 border-b">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <% marcas.forEach((marca, index) => { %>
          <tr class="hover:bg-gray-50">
            <td class="py-2 px-4 border-b"><%= index + 1 %></td>
            <td class="py-2 px-4 border-b"><%= marca.nombre %></td>
            <td class="py-2 px-4 border-b"><%= marca.pais_origen %></td>
            <td class="py-2 px-4 border-b">
              <% if (marca.imagen_marca) { %>
                <img src="/uploads/<%= marca.imagen_marca %>" alt="Marca" class="h-12 object-cover">
              <% } else { %>
                <span class="text-gray-400">Sin imagen</span>
              <% } %>
            </td>
            <td class="py-2 px-4 border-b">
              <a href="/marcas/<%= marca.id_marca %>/edit" class="text-blue-600 hover:underline mr-2">Editar</a>
              <form action="/marcas/<%= marca.id_marca %>" method="POST" class="inline">
                <button type="submit" class="text-red-600 hover:underline">Eliminar</button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>

</body>
</html>
